/**
 * Shared compound CSS className constants.
 *
 * Extracted from repeated inline Tailwind strings to reduce duplication
 * and ensure visual consistency across the UI.
 */

/** Standard interactive list-item button (used in menus, empty-state panels). */
export const LIST_ITEM_CLASS =
	'w-full flex items-center gap-3 px-3 py-2.5 rounded-md hover:bg-white/10 transition-colors text-left';

/** Section / field label used across settings, modals, and forms. */
export const SECTION_LABEL_CLASS = 'block text-xs font-bold opacity-70 uppercase mb-2';
